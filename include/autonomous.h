#ifndef AUTON_H
#define AUTON_H

namespace auton{
  void Half1(); // spins the roller that's already set up
  void Half1Discs(); // additional module for half1() that launches discs
  void Half2(); // setup in corner of tile
}

#endif